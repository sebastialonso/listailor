<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.association :tags, collection: @tags, as: :check_boxes %>
  <%= f.submit "Crear", :class => "btn btn-lg btn-success" %>
<% end %>
